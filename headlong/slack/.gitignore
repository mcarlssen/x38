# Slack CLI runtime state: app links, project id, and cache are per-workspace
# and per-developer. Run `slack install` / `slack app link` yourself; nothing
# in .slack/ is shared through git.
.slack/
.slack/cache/
.venv/
